name: Auto Head Inject

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '**.html'
      - '**head-inject.js'

jobs:
  inject:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Show repo structure
        run: find . -name "*.js" -o -name "*.html" | head -40

      - name: Run head injector
        run: |
          SCRIPT=$(find . -name "head-inject.js" | head -1)
          echo "Found script at: $SCRIPT"
          node "$SCRIPT"

      - name: Commit patched files
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || git commit -m "chore: auto head-inject [skip ci]"
          git push
